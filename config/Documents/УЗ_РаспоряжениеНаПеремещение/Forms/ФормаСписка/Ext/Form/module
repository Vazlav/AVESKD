
Процедура ДействияФормыГрупповоеАннулирование(Кнопка)
	
	ВнешняяОбработка = Справочники.ВнешниеОтчетыИОбработки.НайтиПоКоду(916);
	Если ЗначениеЗаполнено(ВнешняяОбработка) Тогда 		
		ФормаОбработки = ВнешниеОбработки.ПолучитьФорму(ВнешняяОбработка.ПутьКФайлу);
		ФормаОбработки.Открыть();		
	КонецЕсли;
	
КонецПроцедуры
