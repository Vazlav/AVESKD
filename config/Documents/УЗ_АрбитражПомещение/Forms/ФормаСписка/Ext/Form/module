
Процедура ДокументСписокПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки.СтатусДокАптеки = Перечисления.СтатусДокАптеки.Аннулирован  Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,179,179);
	КонецЕсли;
	
	
КонецПроцедуры

Процедура СкладПриИзменении(Элемент)

	ЕстьОтборПоСкладу=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.Склад.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.Склад.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.Склад.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Отбор.Склад.Использование=ЕстьОтборПоСкладу;
	
КонецПроцедуры

Процедура ПодтипПриИзменении(Элемент)
	
	ЕстьОтборПоТипу = ЗначениеЗаполнено(Элемент.Значение);
	Отбор.Подтип.ВидСравнения	= ВидСравнения.Равно;
	Отбор.Подтип.Использование	= ЕстьОтборПоТипу;
	
КонецПроцедуры

Процедура ПричинаПриИзменении(Элемент)
	
	ЕстьОтборПоПричине= ЗначениеЗаполнено(Элемент.Значение);
	Отбор.Причина.ВидСравнения	= ВидСравнения.Равно;
	Отбор.Причина.Использование	= ЕстьОтборПоПричине;
	
КонецПроцедуры
