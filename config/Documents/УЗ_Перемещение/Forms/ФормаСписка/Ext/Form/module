
Процедура ДокументСписокПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.Аннулирован или ДанныеСтроки.СтатусДокАптекиОтправителя = Перечисления.СтатусДокАптеки.Аннулирован Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,179,179);
	ИначеЕсли ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.ОжидаетСогласования Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,246,163);
	ИначеЕсли ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.НазначениеПеревозчика Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,231,146);	
	ИначеЕсли ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.ВПути или ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.ОжидаетОтгрузку Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(220,251,220);
	ИначеЕсли ДанныеСтроки.Статус = Перечисления.СтатусыПеремещения.Завершен Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(200,255,200);		
	КонецЕсли;
	
КонецПроцедуры

Процедура СкладОтправительПриИзменении(Элемент)
	
	ЕстьОтборПоСкладу=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.СкладОтправитель.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.СкладОтправитель.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.СкладОтправитель.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Отбор.СкладОтправитель.Использование=ЕстьОтборПоСкладу;	
	
КонецПроцедуры

Процедура СкладПолучательПриИзменении(Элемент)
	
	ЕстьОтборПоСкладу=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.СкладПолучатель.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.СкладПолучатель.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.СкладПолучатель.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Отбор.СкладПолучатель.Использование=ЕстьОтборПоСкладу;
	
	
КонецПроцедуры

Процедура СбросОтборовНажатие(Элемент)
	
	Отбор.СкладОтправитель.Использование=Ложь;
	Отбор.СкладПолучатель.Использование=Ложь;

	ЭлементыФормы.СкладОтправитель.Значение = Справочники.МестаХранения.ПустаяСсылка();
	ЭлементыФормы.СкладПолучатель.Значение 	 = Справочники.МестаХранения.ПустаяСсылка();
	
	
КонецПроцедуры
