
Процедура ДокументСписокПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки.СтатусДокАптеки = Перечисления.СтатусДокАптеки.Аннулирован Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,179,179);
	ИначеЕсли ДанныеСтроки.СтатусЗаказаПокупателя = Справочники.СтатусыЗаказаПокупателя.SOLD Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(200,255,200);
	КонецЕсли;
	
	
КонецПроцедуры

Процедура ИсточникИнтернетЗаказаПриИзменении(Элемент)
	
	ЕстьОтбор=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.ИсточникИнтернетЗаказа.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.ИсточникИнтернетЗаказа.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.ИсточникИнтернетЗаказа.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Отбор.ИсточникИнтернетЗаказа.Использование=ЕстьОтбор;	
	
КонецПроцедуры

Процедура СтатусЗаказаПокупателяПриИзменении(Элемент)
	
	ЕстьОтбор=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.СтатусЗаказаПокупателя.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.СтатусЗаказаПокупателя.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.СтатусЗаказаПокупателя.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Отбор.СтатусЗаказаПокупателя.Использование=ЕстьОтбор;	
	
КонецПроцедуры

Процедура НомерИнтернетЗаказаПриИзменении(Элемент)
	
	ЕстьОтбор=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.НомерИнтернетЗаказа.Значение)=Тип("СписокЗначений") ТОгда
		Отбор.НомерИнтернетЗаказа.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Отбор.НомерИнтернетЗаказа.ВидСравнения=ВидСравнения.Содержит;
	КонецЕсли; 
	
	Отбор.НомерИнтернетЗаказа.Использование=ЕстьОтбор;	
	
КонецПроцедуры
