
Процедура ПриОткрытии()
	Если ЭтотОбъект.ЭтоНовый()=Истина		Тогда
		АвторЗаявки=ПараметрыСеанса.ТекущийСотр;
		СтатусЗаявки=Перечисления.СтатусыЗаявокНаДоработку.Заявка; 
	КонецЕсли;
	
	Если СтатусЗаявки=Перечисления.СтатусыЗаявокНаДоработку.Готово или
		 СтатусЗаявки=Перечисления.СтатусыЗаявокНаДоработку.Отклонено Тогда
		 ЭтаФорма.ТолькоПросмотр=Истина;
	 КонецЕсли;
	 
		
КонецПроцедуры
