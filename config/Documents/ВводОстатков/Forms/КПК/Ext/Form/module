
Процедура ОсновныеДействияФормыЗаписать(Кнопка)
	Док=Документ.ПолучитьОбъект();
	
	Док.ВхНомерНакл=ВхНомерНакл;
	Док.ВхДатаНакл=ВхДатаНакл;
	
	
	
	Для Каждого Стр Из Товар Цикл
		СтрДок=Док.Товар.Добавить();
		СтрДок.Товар=Стр.Товар;
		СтрДок.Баркод=Стр.Баркод;
		СтрДок.Количество=Стр.Количество;
		СтрДок.КоличествоФакт=Стр.Количество;
	КонецЦикла;
	Док.Записать(РежимЗаписиДокумента.Запись);
	
КонецПроцедуры

Процедура ТоварБаркодОкончаниеВводаТекста(Элемент, Текст, Значение, СтандартнаяОбработка)
	// Поиск товара по баркоду
	
	
КонецПроцедуры


Процедура ОбработкаВнешнегоСобытия(Источник, Событие, Данные)
         сообщить(""+Источник+", "+Событие+", "+Данные);
КонецПроцедуры