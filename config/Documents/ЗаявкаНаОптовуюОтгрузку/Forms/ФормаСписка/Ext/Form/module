
Процедура ПриОткрытии()
	
	Отбор.ПометкаУдаления.Значение = Ложь;
	Отбор.ПометкаУдаления.ВидСравнения=ВидСравнения.Равно;
	Отбор.ПометкаУдаления.Использование=Истина;
	
КонецПроцедуры

Процедура ДокументСписокПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки.Статус = Перечисления.СтатусыЗаявкиНаОтгрузку.СогласованЦО И ДанныеСтроки.Статус = Перечисления.СтатусыЗаявкиНаОтгрузку.СогласованЦО Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,246,163);
	ИначеЕсли  ДанныеСтроки.Статус = Перечисления.СтатусыЗаявкиНаОтгрузку.Создан Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(220,251,220);
	ИначеЕсли ДанныеСтроки.Статус = Перечисления.СтатусыЗаявкиНаОтгрузку.Проведен или ДанныеСтроки.Статус = Перечисления.СтатусыЗаявкиНаОтгрузку.Закрыт Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(200,255,200);	
	КонецЕсли;
	
	Если ДанныеСтроки.ПометкаУдаления Тогда
		ОформлениеСтроки.ЦветФона=Новый Цвет(255,179,179);
	КонецЕсли;
	
	
КонецПроцедуры


