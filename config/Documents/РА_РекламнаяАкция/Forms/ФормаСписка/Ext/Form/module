
Процедура ДействияФормыВыгрузитьВАптеки(Кнопка)
	
	Для Каждого ТекСтрока Из ЭлементыФормы.ДокументСписок.ВыделенныеСтроки Цикл 
		ТекСтрока.Ссылка.ПолучитьОбъект().Выгрузить();
	КонецЦикла;
	
КонецПроцедуры

Процедура ДействияФормыТестоваяВыгрузка(Кнопка)
	
	Для Каждого ТекСтрока Из ЭлементыФормы.ДокументСписок.ВыделенныеСтроки Цикл 
		ТекСтрока.Ссылка.ПолучитьОбъект().Выгрузить(Истина);
	КонецЦикла;
	
КонецПроцедуры
