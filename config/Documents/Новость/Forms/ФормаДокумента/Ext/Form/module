
Процедура ПриОткрытии()
	//------------------<Новости создаются только администраторами>-------------------GtG----
	Если ПараметрыСеанса.ТекущийСотр.Администратор=Ложь ТОгда
		ЭтаФорма.ТолькоПросмотр=Истина;
	КонецЕсли;	
	
	ЕСли ЭтотОбъект.ЭтоНовый() Тогда
		АктуальнаДо=ТекущаяДата()+Константы.СрокАктуальностиНовостейПоУмолчанию.Получить()*60*60*24;
	КонецЕсли;
	
КонецПроцедуры



