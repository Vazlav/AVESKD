
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого ТекЗапись Из ЭтотОбъект Цикл
		Если Не ЗначениеЗаполнено(ТекЗапись.ДатаВнесения) Тогда 
			ТекЗапись.ДатаВнесения = ТекущаяДата();	
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(ТекЗапись.КемВнесен) Тогда 
			ТекЗапись.КемВнесен = ПараметрыСеанса.ТекущийСотр;	
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
