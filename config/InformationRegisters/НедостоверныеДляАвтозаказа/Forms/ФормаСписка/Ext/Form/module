
Процедура ДействияФормыЗагрузитьИзФайла(Кнопка)
	
	Форма = РегистрыСведений.НедостоверныеДляАвтозаказа.ПолучитьФорму("ФормаЗагрузки");
	ЗагрузкаПроизведена = Форма.ОткрытьМодально() = 1;
	                                                 
	Если ЗагрузкаПроизведена Тогда
		Обновить();
	КонецЕсли;
	
КонецПроцедуры

Процедура ДействияФормыУдалитьЗаписи(Кнопка)
	
	ПериодОтбора = НачалоДня(ТекущаяДата());
	Если ВвестиДату(ПериодОтбора, "Введите дату. Более ранние записи будут удалены") Тогда
		
		Выборка = РегистрыСведений.НедостоверныеДляАвтозаказа.Выбрать();
		
		Пока Выборка.Следующий() Цикл
			
			Если Выборка.ДатаБлокировки < ПериодОтбора Тогда
				ТекЗапись = Выборка.ПолучитьМенеджерЗаписи();
				ТекЗапись.Удалить();
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры
