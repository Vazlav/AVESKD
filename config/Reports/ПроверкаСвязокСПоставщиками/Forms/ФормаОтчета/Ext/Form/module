
Процедура ДействияФормыСопоставитьСтроку(Кнопка)
	
	ТекОбласть		= ЭлементыФормы.Результат.ВыделенныеОбласти[0];
	текСтр			= ЭлементыФормы.Результат.Область(ТекОбласть.Верх, 2);
	УсловиеФильтра	= Новый СписокЗначений;
	УсловиеФильтра.Добавить(СокрЛП(текСтр.Текст));
	
	Расшифровка		= ЭлементыФормы.Результат.Область(ТекОбласть.Верх, 5).Расшифровка;
	Поставщик		= ДанныеРасшифровки.Элементы[Расшифровка].ПолучитьПоля()[0].Значение;
	
	Связки					= Обработки.УстановкаСвязокСПоставщикомНовая.Создать();
	Связки					= Связки.ПолучитьФорму("УстановкаСвязокСПоставщикомНовая",, Новый УникальныйИдентификатор);
	Связки.УсловиеФильтра	= УсловиеФильтра;
	Связки.Поставщик		= Поставщик;
	Связки.ОткрытьМодально();
	
КонецПроцедуры
