
Процедура КнопкаВыполнитьНажатие(Кнопка)
	// Вставить содержимое обработчика.
	
	Если Склад.Пустая() Тогда
		Предупреждение("Не выбран склад!");
		Возврат;
	КонецЕсли;	
	
	Для ы=НомерПервогоДобавляемогоСтеллажа по КолвоСтеллажей+НомерПервогоДобавляемогоСтеллажа-1 цикл
		ДЛя э=1 по КолвоСекцийВСтеллаже цикл
			ДЛя Ю=1 По КолвоПолокВСекции Цикл
				
				ЭКС=Справочники.КартаСклада.СоздатьЭлемент();
				
				Экс.Владелец=Склад.Ссылка;
				
				ЭКС.Зона=Зона;
				ЭКС.Стеллаж=Ы;
				ЭКС.Секция=Э;
				ЭКС.Полка=Ю;
				
				ЭКС.Записать();// при записи произойдет генерация наименования
			
		    КонецЦикла; // Полки
		КонецЦикла;// секции
	КонецЦикла;//стеллажи	
	
	
		
	
КонецПроцедуры
