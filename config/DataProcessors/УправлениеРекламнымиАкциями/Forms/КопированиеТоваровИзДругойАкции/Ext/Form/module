
Процедура ОсновныеДействияФормыСкопировать(Кнопка)
	
	ЭтаФорма.ВладелецФормы.СписокОтобранныхПозиций.Очистить();
	
	Если РекламнаяАкция.Пустая()=Истина ТОгда
		Предупреждение("Не выбрана рекламная акция, из которой копировать товары!");
		Возврат;
	КонецЕсли;	
	ЭтаФорма.ВладелецФормы.АкцияИсточникТовара=РекламнаяАкция;

		
		
    ЭтаФорма.Закрыть();
	
	
	
	
КонецПроцедуры

Процедура РекламнаяАкцияПриИзменении(Элемент)
	Если РекламнаяАкция=ТекущаяАкция Тогда
		Предупреждение("Нельзя копировать из одной акции в неё же!");
		РекламнаяАкция=Неопределено;
	КонецЕсли;	
КонецПроцедуры

Процедура ПриОткрытии()
	
	
	
	
	//ЭтаФорма.ВладелецФормы.СписокОтобранныхПозиций.Очистить();
	
	Сообщить(ТекущаяАкция);
	
	
	
КонецПроцедуры


