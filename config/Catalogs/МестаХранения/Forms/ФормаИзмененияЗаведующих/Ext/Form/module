
Процедура ОсновныеДействияФормыкнЗаписать(Кнопка) 	
	НаборЗаписейЗаведующие = РегистрыСведений.Neti_Управляющие.СоздатьНаборЗаписей();
	НаборЗаписейЗаведующие.Отбор.Период.Установить(ТекущаяДата());
	НаборЗаписейЗаведующие.Отбор.КодАптеки.Установить(КодАптеки);
	НаборЗаписейЗаведующие.Загрузить(Заведующие);
	Для i = 0 По НаборЗаписейЗаведующие.Количество()-1 Цикл
		НаборЗаписейЗаведующие[i].Номер = i + 1;
		НаборЗаписейЗаведующие[i].Период = ТекущаяДата();
		НаборЗаписейЗаведующие[i].КодАптеки = КодАптеки;
	КонецЦикла;	
	НаборЗаписейЗаведующие.Записать(Истина); 	
	ОповеститьОВыборе(Заведующие);     	
КонецПроцедуры

Процедура ПриОткрытии()
	АктуальныеЗаведующиеИзРегистра = Справочники.МестаХранения.АктуальныеЗаведующиеИзРегистра(КодАптеки);	
	Для Каждого Элемент Из АктуальныеЗаведующиеИзРегистра Цикл
		НоваяСтрокаЗаведующие = Заведующие.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрокаЗаведующие, Элемент);
	КонецЦикла;	
КонецПроцедуры
