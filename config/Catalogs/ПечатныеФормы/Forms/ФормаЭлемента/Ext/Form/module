
Процедура Кнопка1Нажатие(Элемент)
	// Вставить содержимое обработчика.
	СЗ=Новый СписокЗначений;
	
	Для каждого дкм Из Метаданные.Документы Цикл
		
		СЗ.Добавить(Дкм.Синоним);
		
	КонецЦикла;
	
	ЭСЗ=СЗ.ВыбратьЭлемент("Документ?", Объект);
	Объект=ЭСЗ.Значение;
	
	
	
КонецПроцедуры

Процедура Кнопка2Нажатие(Элемент)
	// Вставить содержимое обработчика.
	
	ДВФ=Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	ДВФ.МножественныйВыбор=Ложь;
	ДВФ.Заголовок="Обработка печати?";
	ДВФ.Фильтр="*.epf|*.epf";
	ДВФ.Расширение="epf";
	Если ДВФ.Выбрать() Тогда
		ПутьКФайлу=СокрЛП(ДВФ.ПолноеИмяФайла);
	КонецЕсли;
	
КонецПроцедуры
