

Процедура СправочникСписокПередОкончаниемРедактирования(Элемент, НоваяСтрока, ОтменаРедактирования, Отказ)
	// Вставить содержимое обработчика.
		ИмяТекКол=Элемент.ТекущаяКолонка.Имя;
	
	Если ИмяТекКол="Администратор" Тогда
		Пароль="";
		
		Если ВвестиСтроку(Пароль,"Пароль?",10,Ложь) ТОгда
			Если Пароль<>"1925624" Тогда
				 ОтменаРедактирования= ИСТИНА;
				 
			 КонецЕсли; 
			 
		Иначе
			ОтменаРедактирования= ИСТИНА;
			
		КонецЕсли; 	
	КонецЕсли; 
КонецПроцедуры


