
Процедура ПриОткрытии()
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ГруппыНаценки.Ссылка
	               |ИЗ
	               |	Справочник.ГруппыНаценки КАК ГруппыНаценки
	               |ГДЕ
	               |	НЕ ГруппыНаценки.Ссылка В(&СписокСуществующихГрупп)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	ГруппыНаценки.Наименование";
	Запрос.УстановитьПараметр("СписокСуществующихГрупп",СписокСуществующихГрупп);
	Рез = Запрос.Выполнить();
	Выборка = Рез.Выбрать();
	Пока Выборка.Следующий() Цикл
		СписокГруппНаценок.Добавить(Выборка.Ссылка,,Ложь);	
	КонецЦикла;
	
	
КонецПроцедуры

Процедура ОсновныеДействияФормыВыбратьОтмеченныеГруппы(Кнопка)
	
	СписокВозврата = Новый СписокЗначений;
	Для каждого стр из СписокГруппНаценок Цикл
		Если стр.Пометка = Истина Тогда
			СписокВозврата.Добавить(стр.Значение);	
		КонецЕсли;
	КонецЦикла;
	ОповеститьОВыборе(СписокВозврата);
	
КонецПроцедуры

Процедура КоманднаяПанель2Действие(Кнопка)
	
	СписокГруппНаценок.ЗаполнитьПометки(Истина);
	
КонецПроцедуры

Процедура КоманднаяПанель2Действие1(Кнопка)
	
	СписокГруппНаценок.ЗаполнитьПометки(Ложь);
	
КонецПроцедуры
