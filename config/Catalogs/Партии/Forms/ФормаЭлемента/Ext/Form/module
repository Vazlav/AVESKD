
Процедура ПриОткрытии()
		
	ОПартия=РСРЦ.Отбор.Найти("Партия");
	ОПартия.Значение= ЭтотОбъект.Ссылка;
	ОПартия.ВидСравнения =ВидСравнения.Равно;
	ОПартия.Использование =Истина;
	попытка
		ЭлементыФормы.НадписьФирма.Заголовок=ДокументПоступления.Фирма;
	исключение
		ЭлементыФормы.НадписьФирма.Заголовок="ЭТО ПАРТИЯ-ЗАТЫЧКА. ФИРМЫ НЕТ!";
	КонецПопытки;
	
КонецПроцедуры

Процедура ДействияФормыДвижениеПратииТовара(Кнопка)
		
	Карточка=Отчеты.ДвижениеТовара1.Создать();
	
	Карточка.ВыбПартия=ЭтотОбъект.Ссылка;
	
	Карточка.ВыбСклад="";
	
	Карточка.ВыбТовар=ЭтотОбъект.Владелец;
	Карточка.ВыбФирма=ЭтотОбъект.ДокументПоступления.Фирма;
	Карточка.НачПер=ЭтотОбъект.ДокументПоступления.Дата;
	Карточка.КонПер=ТекущаяДата();
	
	
	Ф=Карточка.ПолучитьФорму("Форма");
	
	Ф.Открыть();

КонецПроцедуры
ЭтаФорма.Заголовок=ЭтотОбъект.Владелец;





