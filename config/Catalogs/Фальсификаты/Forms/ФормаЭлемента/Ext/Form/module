Перем СписокРешенийПоВозврату;


Процедура РешениеПоВозвратуНачалоВыбораИзСписка(Элемент, СтандартнаяОбработка)
	
	Элемент.СписокВыбора = СписокРешенийПоВозврату;
	
КонецПроцедуры

Процедура ИндивидуальнаяПозицияНачалоВыбора(Элемент, СтандартнаяОбработка)
	//ENT-649
	Если НЕ АП.Пустая() Тогда
		СтандартнаяОбработка = Ложь;
		ФормаВыбора = Справочники.ИндивидуальнаяПозиция.ПолучитьФормуВыбора(, Элемент);
		Отбор = ФормаВыбора.СправочникСписок.Отбор.АссортиментныйПлан;
		Отбор.Использование = Истина;
		Отбор.ВидСравнения = ВидСравнения.Равно;
		Отбор.Значение = АП;
		ФормаВыбора.Открыть();
	КонецЕсли;
КонецПроцедуры

Процедура ИндивидуальнаяПозицияОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	//ENT-649
	Если ПустаяСтрока(Производитель) Тогда
		Производитель = ВыбранноеЗначение.Производитель.Наименование;
	КонецЕсли;
	Если ПустаяСтрока(Страна) Тогда
		Страна = ВыбранноеЗначение.Страна.Наименование;
	КонецЕсли;
	
	АП = ВыбранноеЗначение.АссортиментныйПлан;
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	//ENT-649
	//Если ИндивидуальнаяПозиция.Пустая() Тогда
	//	Сообщить("Не выбрана Индивидуальная позиция",СтатусСообщения.ОченьВажное);
	//	Отказ = Истина;
	//КонецЕсли;
	//Если (СрокГодности = Дата(1,1,1)) и (Не ПустаяСтрока(НомерСерии)) Тогда
	//	Сообщить("Не заполнен Срок годности",СтатусСообщения.ОченьВажное);
	//	Отказ = Истина;
	//КонецЕсли;
	
КонецПроцедуры

СписокРешенийПоВозврату = новый СписокЗначений;
СписокРешенийПоВозврату.Добавить("Возврат");
СписокРешенийПоВозврату.Добавить("Приостановка");
СписокРешенийПоВозврату.Добавить("Разбраковка");//Самсонов 20.06.18 SKD-208 