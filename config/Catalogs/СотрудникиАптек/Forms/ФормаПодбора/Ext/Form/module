
Процедура кнПеренестиНажатие(Элемент)
	
	МожноЗакрыть= ИСТИНА ;
	ОповеститьОВыборе(ТЗПодбора);

КонецПроцедуры

Процедура АптекаПриИзменении(Элемент)
	
	ОтборПоАптеке=СправочникСписок.Отбор.Найти("Владелец");
	ОтборПоАптеке.Установить(Аптека,Истина);
	Если Аптека.Пустая() Тогда
		ОтборПоАптеке.Использование = Ложь;
	Иначе
		ОтборПоАптеке.Использование = Истина;
	КонецЕсли;
	//ОтборПоСкладу=СписокЗаказов.Отбор.Найти("Склад");
	//ОтборПоСкладу.Установить(ТекАптека,Истина);
	
	
КонецПроцедуры

Процедура ДолжностьПриИзменении(Элемент)
	
	ОтборПоДолжности=СправочникСписок.Отбор.Найти("Должность");
	ОтборПоДолжности.Установить(Должность,Истина);
	Если Должность.Пустая() Тогда
		ОтборПоДолжности.Использование = Ложь;
	Иначе
		ОтборПоДолжности.Использование = Истина;
	КонецЕсли;
	
	
КонецПроцедуры

Процедура СправочникСписокВыбор(Элемент, ВыбраннаяСтрока, Колонка, СтандартнаяОбработка)
	
	НоваяСтрока = ТЗПодбора.Добавить();
	НоваяСтрока.Сотрудник = ВыбраннаяСтрока.Получить(0).Ссылка;
	НоваяСтрока.Аптека = ВыбраннаяСтрока.Получить(0).Владелец;
	
КонецПроцедуры

МожноЗакрыть=Ложь;