Процедура ДействияФормыкнГрупповаяОбработка(Кнопка)
	
	ВнешняяОбработка = Справочники.ВнешниеОтчетыИОбработки.НайтиПоКоду(1195);
	Если ЗначениеЗаполнено(ВнешняяОбработка) Тогда 		
		ФормаОбработки = ВнешниеОбработки.ПолучитьФорму(ВнешняяОбработка.ПутьКФайлу);
		ФормаОбработки.Открыть();		
	КонецЕсли;
	
КонецПроцедуры