
Процедура ДействияФормыОчиститьВсе(Кнопка)
	Выборка = Справочники.НеотказываемыйТовар.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Объект = Выборка.ПолучитьОбъект();
		Объект.Удалить();
	КонецЦикла;
	
КонецПроцедуры
