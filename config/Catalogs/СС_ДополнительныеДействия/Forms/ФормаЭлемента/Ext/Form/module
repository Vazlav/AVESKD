
Процедура ПараметрыНажатие(Элемент)
	Если ЭтоНовый() ТОгда
		Если Вопрос("Перед редактированием параметров элемент надо записать. Записать", РежимДиалогаВопрос.ДаНет)=КодВозвратаДиалога.Нет ТОгда
			Возврат;
		КонецЕсли;
		Записать();
	КонецЕсли;
	
	РП=ПолучитьФорму("РедакторЗначенийПараметров");
	РП.Владелец=ЭтотОбъект;
	РП.Параметры=ЭтотОбъект.Параметры;
	РП.Открыть(); 
КонецПроцедуры
