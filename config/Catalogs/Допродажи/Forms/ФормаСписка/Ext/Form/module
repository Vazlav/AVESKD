
Процедура СправочникСписокПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	СпрАП = Справочники.АССОРТИМЕНТНЫЙ_ПЛАН;
	
	КодТовара = ДанныеСтроки.КодТовара;
	СпрАПСсылка = СпрАП.НайтиПоКоду(КодТовара);
	Если НЕ СпрАПСсылка = СпрАП.ПустаяСсылка() Тогда
		ОформлениеСтроки.Ячейки.НаименованиеТовара.УстановитьТекст(СпрАПСсылка.Наименование);
	КонецЕсли; 
	
	КодТовара = ДанныеСтроки.КодТовараДопродажи;
	СпрАПСсылка = СпрАП.НайтиПоКоду(КодТовара);
	Если НЕ СпрАПСсылка = СпрАП.ПустаяСсылка() Тогда
		ОформлениеСтроки.Ячейки.НаименованиеТовараДопродажи.УстановитьТекст(СпрАПСсылка.Наименование);
	КонецЕсли; 

КонецПроцедуры

Процедура СправочникСписокКодТовараПриИзменении(Элемент)
	
	СпрАП = Справочники.АССОРТИМЕНТНЫЙ_ПЛАН;
	ТекущаяСтрока = Элемент.ТекущаяСтрока;
	КодТовара = ТекущаяСтрока.КодТовара;

	СпрАПСсылка = СпрАП.НайтиПоКоду(КодТовара);
	Если НЕ СпрАПСсылка = СпрАП.ПустаяСсылка() Тогда
		ТекущаяСтрока.НаименованиеТовара = СпрАПСсылка.Наименование;
	КонецЕсли; 
	
КонецПроцедуры

Процедура СправочникСписокКодТовараДопродажиПриИзменении(Элемент)
	
	СпрАП = Справочники.АССОРТИМЕНТНЫЙ_ПЛАН;
	ТекущаяСтрока = Элемент.ТекущаяСтрока;
	КодТовара = ТекущаяСтрока.КодТовара;

	СпрАПСсылка = СпрАП.НайтиПоКоду(КодТовара);
	Если НЕ СпрАПСсылка = СпрАП.ПустаяСсылка() Тогда
		ТекущаяСтрока.НаименованиеТовара = СпрАПСсылка.Наименование;
	КонецЕсли; 
	
КонецПроцедуры
