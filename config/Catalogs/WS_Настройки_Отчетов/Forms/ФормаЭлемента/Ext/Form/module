
Процедура ТекстыЗапросовТекстЗапросаОткрытие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка=ЛОжь;
	
	Ф=ЭтотОбъект.ПолучитьФорму("ФормаЗапроса",ЭтаФорма,1);
	Ф.СтрокаНастроек=ЭлементыФормы.ТекстыЗапросов.ТекущаяСтрока;
	Ф.РеквТекстЗапроса= Элемент.Значение;
	Ф.Открыть();
	
КонецПроцедуры

Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	// Вставить содержимое обработчика.
	
	Если ИмяСобытия="ТекстЗапросаИзменен" Тогда
		Параметр.СтрокаНастроек.ТекстЗапроса=Параметр.ТекстЗапроса;
	КонецЕсли;	
	
	
КонецПроцедуры
