
Процедура ПередЗаписью(Отказ)
	
	// ==>ENT-1275.Коробка.06.02.2019.Отмена проверки 
	//Если ЭтоНовый() Тогда
	//
	//	Если Не ЗначениеЗаполнено(Поставщик) Тогда
	//	
	//		Сообщить("Не заполнено поле ""Поставщик""!");
	//		Отказ = Истина;
	//	
	//	КонецЕсли; 
	//
	//КонецЕсли;
	// <==ENT-1275.Коробка.06.02.2019.Отмена проверки 
	
	
КонецПроцедуры

// ==>ENT-1275.Коробка.06.02.2019.Добавление роли на справочник 
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если НЕ РольДоступна("РедактированиеПроизводители") и НЕ РольДоступна("супер_АДМИНИСТРАТОР") Тогда
	
		Если ЭтоНовый() Тогда
		
			Сообщить ("У пользователя отсутствует роль ""Редактирование справочника Производители"". Создание нового элемента не доступно!");
			Отказ = Истина;	
			
		Иначе
			
			ЭтаФорма.ТолькоПросмотр = Истина

		КонецЕсли; 		
	
	КонецЕсли; 
	
КонецПроцедуры
// <==ENT-1275.Коробка.06.02.2019.Добавление роли на справочник 

