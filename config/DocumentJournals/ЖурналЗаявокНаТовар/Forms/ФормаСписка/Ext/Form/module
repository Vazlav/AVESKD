
Процедура ЗаявкинатоварПриАктивизацииСтроки(Элемент)
	
	ДокОснованиеСсылка=Элемент.ТекущаяСтрока;
	Если ДокОснованиеСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Заказы.Отбор.Сбросить();
	
	ОтборПоДокОснованию=Заказы.Отбор.Найти("ДокОснование");
	ОтборПоДокОснованию.Значение=ДокОснованиеСсылка;
	ОтборПоДокОснованию.ВидСравнения=ВидСравнения.Равно;
	ОтборПоДокОснованию.Использование = Истина;
	
КонецПроцедуры

Процедура СкладПриИзменении(Элемент)
	
	ЕстьОтборПоСкладу=ЗначениеЗаполнено(Элемент.Значение);
	Если ТипЗнч(ЭлементыФормы.Склад.Значение)=Тип("СписокЗначений") ТОгда
		Заявкинатовар.Отбор.Склад.ВидСравнения=ВидСравнения.ВСписке;
	Иначе
		Заявкинатовар.Отбор.Склад.ВидСравнения=ВидСравнения.Равно;
	КонецЕсли; 
	
	Заявкинатовар.Отбор.Склад.Использование=ЕстьОтборПоСкладу;
	
	
КонецПроцедуры

Процедура кнСброситьФильтрПоАптекеНажатие(Элемент)
	
	Заявкинатовар.Отбор.Склад.Использование=Ложь;
	ЭлементыФормы.Склад.Значение 	 = Справочники.МестаХранения.ПустаяСсылка();
	
	
КонецПроцедуры
